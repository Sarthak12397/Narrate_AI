// During dev we proxy API requests through Vite to the backend, so
// everything appears under one origin: http://localhost:5177.
export const BASE_URL = ""; // use relative URLs like "/upload/file"
